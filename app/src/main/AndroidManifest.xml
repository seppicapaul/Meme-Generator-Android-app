<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ssu.softwarednd.spring19.androidlab2">

    <!-- app permissions -->
    <!-- application components -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

            <!-- specifies the types of intents this activity can respond to -->
            <intent-filter>

                <!-- the main entry point for the application -->
                <action android:name="android.intent.action.MAIN" />
                <!-- put an icon for this activity in the app tray -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectedImage" />
        <activity android:name=".TwoImageTop" />
        <activity android:name=".BackgroundImage" />
        <activity android:name=".ForegroundImage" />
        <activity android:name=".TwoImageBottom" />
        <activity android:name=".TwoImageTwo" />
        <activity android:name=".OneImageTop" />
        <activity android:name=".OneImageBottom" />
        <activity android:name=".GenerateButton" />
        <activity android:name=".ColorButton" />
        <activity android:name=".TextButton" />
        <activity android:name=".SecondTextButton"></activity>
    </application>

</manifest>